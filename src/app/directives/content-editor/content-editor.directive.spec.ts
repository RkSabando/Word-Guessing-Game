import { ContentEditorDirective } from './content-editor.directive';

describe('ContentEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentEditorDirective();
    expect(directive).toBeTruthy();
  });
});
